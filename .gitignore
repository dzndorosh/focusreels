node_modules/
dist/
.DS_Store
adapters/ax/.build/
media/*.mp4
media/*.mov
media/*.webm

# never commit a real key
.env

# Research and maintainer outputs are reproducible local evidence, not runtime input.
artifacts/bluesky-probe/
artifacts/editorial-feed/
artifacts/youtube-catalog/
config/youtube-sources.candidates.json
config/youtube-sources.test.json
# Keep aggregate decisions visible when generated locally.
!artifacts/bluesky-probe/summary.json
!artifacts/editorial-feed/summary.json

# macOS bundle output
out/
build/
release/
*.dmg

# Personal tooling installed by the Next Move Theory installer, not part of this
# product. Present on disk so the agent setup keeps working; never versioned here.
Next-Move-Theory-Canon/
NextMoveTheory-README.md
.nmt-version
CLAUDE.md
.claude/
.agents/
.superpowers/
